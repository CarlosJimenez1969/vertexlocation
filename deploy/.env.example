# /opt/vertexjd/.env — copiar a ".env" y rellenar. NO subir a git.
# Genera contraseñas/secretos fuertes (ej. `openssl rand -base64 24`).

# PostgreSQL (superusuario y base de VertexLocation)
PG_PASSWORD=CAMBIAR_superuser_postgres
VXL_DB_PASSWORD=CAMBIAR_password_base_vertexlocation

# VertexLocation
JWT_SECRET=CAMBIAR_cadena_larga_aleatoria
ADMIN_EMAIL=cijj1969@gmail.com
ADMIN_NAME=Carlos
ADMIN_PASSWORD=CAMBIAR_tu_clave_admin

# Correo (Gmail SMTP)
SMTP_USER=cijj1969@gmail.com
SMTP_FROM=VertexLocation <cijj1969@gmail.com>
SMTP_PASSWORD=CAMBIAR_gmail_app_password
