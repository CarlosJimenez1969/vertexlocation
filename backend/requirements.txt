# ===== VertexMascota — Backend (Python + FastAPI) =====

# --- Framework web / ASGI ---
fastapi==0.115.0
uvicorn[standard]==0.30.6
python-multipart==0.0.9          # subida de archivos (foto de mascota)

# --- Base de datos ---
SQLAlchemy==2.0.35
psycopg2-binary==2.9.9           # driver PostgreSQL
alembic==1.13.2                  # migraciones

# --- Validación / settings ---
pydantic==2.9.2
pydantic-settings==2.5.2
email-validator==2.2.0

# --- Seguridad / Auth ---
PyJWT==2.9.0
passlib[bcrypt]==1.7.4
# bcrypt <4.1 evita el warning "error reading bcrypt version" con passlib 1.7.4
# (passlib lee bcrypt.__about__, removido en bcrypt 4.1+).
bcrypt==4.0.1

# --- Cálculo del estado de ánimo ---
numpy==2.1.1
pandas==2.2.3

# --- Integración Traccar ---
requests==2.32.3

# --- Tiempo real ---
websockets==13.1

# --- Tareas programadas ---
celery==5.4.0
redis==5.0.8

# --- Alertas WhatsApp ---
twilio==9.3.0

# --- Reportes PDF veterinario ---
reportlab==4.2.2

# --- Utilidades ---
python-dotenv==1.0.1
httpx==0.27.2

# --- Tests ---
pytest==8.3.3
pytest-asyncio==0.24.0
